<%= link_to "Back home", root_path %><br>
<% if current_user.lessors.length > 0 %>
    Renting out:
    <% current_user.lessors.each do |lessor| %>
        <%= lessor.lease.item.name %>
        $<%= lessor.lease.price %> per day
    <% end %>
<% end %><br>
<% if current_user.lessees.length > 0 %>
    Renting in:
    <% current_user.lessees.each do |lessee| %>
        <%= lessee.lease.item.name %>,
        $<%= lessee.lease.price %> per day,
        for <%= lessee.lease.duration_days %> days,
        <% if lessee.lease.all_at_once %>
            Paid all at once
        <% else %>
            Paid incrementally
        <% end %>
            

    <% end %>
<% end %>

<% if current_user.lessees.length + current_user.lessors.length == 0 %>
    No leases yet!
<% end %>